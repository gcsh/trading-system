fastapi>=0.118
uvicorn[standard]>=0.32
pydantic>=2.11
sqlalchemy>=2.0.35
python-dotenv>=1.0.1
apscheduler>=3.10.4
robin_stocks>=3.4.0
alpaca-py>=0.30.1
pyotp>=2.9.0
yfinance>=0.2.44
pandas>=2.2.3
numpy>=2.1
ta>=0.11.0
# MITS Phase 0 — candlestick pattern detectors. Wheel ships prebuilt
# for cp310/cp311/cp312/cp314 on macOS+linux. Detectors gracefully
# degrade to [] if the import fails, so a partial install still boots.
ta-lib>=0.6.0
newsapi-python>=0.2.7
vaderSentiment>=3.3.2
websockets>=13.1
httpx>=0.27.2
lightgbm>=4.5
scikit-learn>=1.5
anthropic>=0.40
# MITS Phase 11.1 — DuckDB analytics read layer (httpfs reads parquet
# from S3 directly so heavy aggregate queries don't lock SQLite).
duckdb>=1.1
# MITS Phase 11.1 — PDF extraction for SEC 8-K Exhibit 99.1 fallback
# (most press releases are HTML; some companies file as PDF).
pdfplumber>=0.11
PyPDF2>=3.0
# MITS Phase 11.1 — Bronze ferry parquet serialization.
pyarrow>=17
# MITS Phase 11.1 — memory pressure guard probe (also used by lake
# health monitor for the green/yellow/red chip on Lake Status).
psutil>=5.9
# MITS Phase 11.1 — SEC HTML→text conversion (Exhibit 99.1 press
# releases are HTML; bs4 is also used by other parsers).
beautifulsoup4>=4.12
# MITS Phase 11.K — FinBERT sentiment + sentence-transformers
# embedding (already wired but listed here so a fresh install on a
# new box pulls them).
transformers>=4.40
sentence-transformers>=2.7
psycopg2-binary>=2.9
# MITS Phase 15.C — YAML strategy templates loader.
PyYAML>=6.0
pytest>=8.3.3
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
